\documentclass{ximera}

\input{../../preamble.tex}

\outcome{Identify where a function is, and is not, continuous.}
\outcome{Understand the connection between continuity of a function and the value of a limit.}

\author{Nela Lakos \and Kyle Parsons}

\begin{document}
\begin{exercise}

Consider the function $f$, given by the graph below.

\begin{image}
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-10.3,xmax=10.3,ymin=-10.3,ymax=10.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-10,-8,...,10},
    xtick={-10,-8,...,10},
        xlabel=$x$, ylabel=$y$,
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick,penColor,domain = -10.3:0,samples=50] plot{sin(360*x/pi) - x};
      \addplot[very thick,penColor,domain = 0:3,samples=50] plot{2*(x-3)/((3.3-x)*x)};
      \draw [very thick,penColor] (axis cs:3,0) -- (axis cs:6,2);
      \addplot[very thick,penColor,domain = 6:10.3] plot{2};
       
      \addplot[color=penColor,fill=penColor,only marks,mark=*] coordinates{(0,0)};
      \addplot[color=penColor,fill=penColor,only marks,mark=*] coordinates{(6,-3)};
 
      \addplot[color=penColor,fill=white,only marks,mark=*] coordinates{(6,2)};
      \addplot[color=penColor,fill=white,only marks,mark=*] coordinates{(3,0)};
 
      \node[penColor] at (axis cs:3, 7) [penColor] {$y=f(x)$};
      \end{axis}`
  \end{tikzpicture}
\end{image}

$f$ is \wordChoice{\choice{continuous}\choice[correct]{not continuous}} at $a=0$ because 
\wordChoice{
\choice{$\lim_{x\to a}f(x) = f(a)$}
\choice[correct]{$\lim_{x\to a}f(x)$ does not exist}
\choice{$x=a$ is not in the domain of $f$}
\choice{$\lim_{x\to a}f(x)$ exists but is not $f(a)$}}.

$f$ is \wordChoice{\choice{continuous}\choice[correct]{not continuous}} at $a=3$ because 
\wordChoice{
\choice{$\lim_{x\to a}f(x) = f(a)$}
\choice{$\lim_{x\to a}f(x)$ does not exist}
\choice[correct]{$x=a$ is not in the domain of $f$}
\choice{$\lim_{x\to a}f(x)$ exists but is not $f(a)$}}.

$f$ is \wordChoice{\choice[correct]{continuous}\choice{not continuous}} at $a=\pi$ because 
\wordChoice{
\choice[correct]{$\lim_{x\to a}f(x) = f(a)$}
\choice{$\lim_{x\to a}f(x)$ does not exist}
\choice{$x=a$ is not in the domain of $f$}
\choice{$\lim_{x\to a}f(x)$ exists but is not $f(a)$}}.

$f$ is \wordChoice{\choice{continuous}\choice[correct]{not continuous}} at $a=6$ because 
\wordChoice{
\choice{$\lim_{x\to a}f(x) = f(a)$}
\choice{$\lim_{x\to a}f(x)$ does not exist}
\choice{$x=a$ is not in the domain of $f$}
\choice[correct]{$\lim_{x\to a}f(x)$ exists but is not $f(a)$}}.

\end{exercise}
\end{document}