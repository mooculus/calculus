\documentclass{ximera}

\input{../../preamble.tex}

\outcome{Find the intervals where a function is increasing or decreasing.}
\outcome{Find the intervals where a function is concave up or down.}
\outcome{Find any asymptotic behaviors a function may have: vertical, horizontal, or slant.}
\outcome{Determine how the graph of a function looks without using a calculator.}

\author{Nela Lakos \and Kyle Parsons}

\begin{document}
\begin{exercise}

Consider the function $f(x) = \frac{2x^2}{x^2-1}$.

The domain of $f$ (from left to right) is $\left(\answer{-\infty},\answer{-1}\right)\cup\left(\answer{-1},\answer{1}\right)\cup\left(\answer{1},\answer{\infty}\right)$.

$f$ has a horizontal asymptote at $y=\answer{2}$.

$f$ has vertical asymptotes (from left to right) at $x=\answer{-1}$ and $x=\answer{1}$.

The derivative of $f$ is given by 
\[
f'(x) =\frac{ \answer{-4x}}{(x^2-1)^2}.
\]

The second derivative of $f$ is given by
\[
f''(x) =\frac{ \answer{12x^2+4}}{(x^2-1)^3}.
\]

$f$ is increasing and concave up on $\left(\answer{-\infty},\answer{-1}\right)$.

$f$ is increasing and concave down on $\left(\answer{-1},\answer{0}\right)$.

$f$ is decreasing and concave up on $\left(\answer{1},\answer{\infty}\right)$.

$f$ is decreasing and concave down on $\left(\answer{0},\answer{1}\right)$.

$f$ has a local maximum at $x=\answer{0}$.

$f$ has $\answer{0}$ local minimums.

$f$ \wordChoice{\choice{has}\choice[correct]{does not have}} inflection points at $x=-1$ and $x=1$.

Consider the graphs below.

\resizebox{0.45\textwidth}{!}{
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-4.3,xmax=4.3,ymin=-3.3,ymax=5.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-20,-19,...,20},
    	xtick={-20,-19,...,20},
        xlabel=$x$, ylabel=$y$,
        y tick label style={anchor=west},
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick,penColor,domain=-4.3:-1] plot{2*x^2/(x^2-1)};
      \addplot[very thick,penColor,domain=-.9:.9] plot{2*x^2/(x^2-1)+1};            
      \addplot[very thick,penColor,domain=1:4.3] plot{2*x^2/(x^2-1)};
            
      \node at (axis cs:2.5,-1.5) {\huge $A$};
      \end{axis}`
  \end{tikzpicture}}
\hfill
\resizebox{0.45\textwidth}{!}{
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-4.3,xmax=4.3,ymin=-3.3,ymax=5.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-20,-19,...,20},
    	xtick={-20,-19,...,20},
        xlabel=$x$, ylabel=$y$,
        y tick label style={anchor=west},
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick,penColor,domain=-4.3:-1] plot{-2*x^3/(x^2-1)-4};
      \addplot[very thick,penColor,domain=-.9:.9] plot{2*x^2/(x^2-1)};            
      \addplot[very thick,penColor,domain=1:4.3] plot{2*x^3/(x^2-1)-4};
      
      \node at (axis cs:2.5,-1.5) {\huge $B$};
      \end{axis}`
  \end{tikzpicture}}

\resizebox{0.45\textwidth}{!}{
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-4.3,xmax=4.3,ymin=-3.3,ymax=5.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-20,-19,...,20},
    	xtick={-20,-19,...,20},
        xlabel=$x$, ylabel=$y$,
        y tick label style={anchor=west},
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick,penColor,domain=-4.3:-1] plot{2*x^2/(x^2-1)};
      \addplot[very thick,penColor,domain=-.9:.9] plot{2*x^2/(x^2-1)};            
      \addplot[very thick,penColor,domain=1:4.3] plot{2*x^2/(x^2-1)};
      
      \node at (axis cs:2.5,-1.5) {\huge $C$};
      \end{axis}`
  \end{tikzpicture}}
\hfill
\resizebox{0.45\textwidth}{!}{
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-4.3,xmax=4.3,ymin=-3.3,ymax=5.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-20,-19,...,20},
    	xtick={-20,-19,...,20},
        xlabel=$x$, ylabel=$y$,
        y tick label style={anchor=west},
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick,penColor,domain=-4.3:-1] plot{2*x^2/(x^2-1)-2};
      \addplot[very thick,penColor,domain=-.9:.9] plot{2*x^2/(x^2-1)};            
      \addplot[very thick,penColor,domain=1:4.3] plot{2*x^2/(x^2-1)-2};
      
      \node at (axis cs:2.5,-1.5) {\huge $D$};
      \end{axis}`
  \end{tikzpicture}}

Chose the graph that correctly depicts $f(x)$.
\begin{multipleChoice}
\choice{$A$}
\choice{$B$}
\choice[correct]{$C$}
\choice{$D$}
\end{multipleChoice}


\end{exercise}
\end{document}