\documentclass{ximera}

\input{../../preamble.tex}

\outcome{Find the intervals where a function is increasing or decreasing.}
\outcome{Find the intervals where a function is concave up or down.}
\outcome{Find any asymptotic behaviors a function may have: vertical, horizontal, or slant.}
\outcome{Determine how the graph of a function looks without using a calculator.}

\author{Nela Lakos \and Kyle Parsons}

\begin{document}
\begin{exercise}

Consider the function $f$ given by
\[
f(x) = x + \frac{1}{x}.
\]

The domain of $f$ (from left to right) is $\left(\answer{-\infty},\answer{0}\right)\cup\left(\answer{0},\answer{\infty}\right)$.

$f$ is \wordChoice{\choice{even}\choice[correct]{odd}\choice{neither even nor odd}}.

$f$ has a horizontal asymptote at $y = \answer{DNE}$. (Write \verb|DNE| if $f$ doesn't have a horizontal asymptote.)

$f$ has a vertical asymptote at $x = \answer{0}$. (Write \verb|DNE| if $f$ doesn't have a vertical asymptote.)

The derivative of $f$ is 
\[
f'(x) = \answer{1-\frac{1}{x^2}}.
\]

The second derivative of $f$ is 
\[
f''(x) = \answer{\frac{2}{x^3}}.
\]

$f$ is increasing and concave down on $\left(\answer{-\infty},\answer{-1}\right)$.

$f$ is increasing and concave up on $\left(\answer{1},\answer{\infty}\right)$.

$f$ is decreasing and concave down on $\left(\answer{-1},\answer{0}\right)$.

$f$ is decreasing and concave up on $\left(\answer{0},\answer{1}\right)$.

$f$ has a local minimum at $x=\answer{1}$.

$f$ has a local maximum at $x=\answer{-1}$.

$f$ \wordChoice{\choice{has some}\choice[correct]{doesn't have any}} inflection points.

Consider the graphs below.

\resizebox{0.45\textwidth}{!}{
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-5.3,xmax=5.3,ymin=-5.3,ymax=5.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-20,-19,...,20},
    	xtick={-20,-19,...,20},
        xlabel=$x$, ylabel=$y$,
        y tick label style={anchor=west},
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick,penColor,domain=-5.3:-.999,samples=500] plot{x/sqrt(x^2-1)};
      \addplot[very thick,penColor,domain=1.001:5.3,samples=500] plot{x/sqrt(x^2-1)};
            
      \node at (axis cs:2.5,-2.5) {\huge$A$};
      \end{axis}`
  \end{tikzpicture}}
\hfill
\resizebox{0.45\textwidth}{!}{
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-5.3,xmax=5.3,ymin=-5.3,ymax=5.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-20,-19,...,20},
    	xtick={-20,-19,...,20},
        xlabel=$x$, ylabel=$y$,
        y tick label style={anchor=west},
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick,penColor,domain=-5.3:-0.1,samples=50] plot{x + 1/x};
      \addplot[very thick,penColor,domain=0.1:5.3,samples=50] plot{x + 1/x};
      
      \node at (axis cs:2.5,-2.5) {\huge$B$};
      \end{axis}`
  \end{tikzpicture}}

\resizebox{0.45\textwidth}{!}{
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-5.3,xmax=5.3,ymin=-5.3,ymax=5.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-20,-19,...,20},
    	xtick={-20,-19,...,20},
        xlabel=$x$, ylabel=$y$,
        y tick label style={anchor=west},
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick,penColor,domain=-6.3:6.3,samples=120] plot{5*x*(x-1)*(x+1)/(x^4+1)};
            
      \node at (axis cs:2.5,-2.5) {\huge$C$};
      \end{axis}`
  \end{tikzpicture}}
\hfill
\resizebox{0.45\textwidth}{!}{
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-5.3,xmax=5.3,ymin=-5.3,ymax=5.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-20,-19,...,20},
    	xtick={-20,-19,...,20},
        xlabel=$x$, ylabel=$y$,
        y tick label style={anchor=west},
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick,penColor,domain=-3.3:3.3,samples=50] plot{2*x*(x-1)*(x+1)};
      
      \node at (axis cs:2.5,-2.5) {\huge$D$};
      \end{axis}`
  \end{tikzpicture}}

Chose the graph that correctly depicts $f(x)$.
\begin{multipleChoice}
\choice{$A$}
\choice[correct]{$B$}
\choice{$C$}
\choice{$D$}
\end{multipleChoice}


\end{exercise}
\end{document}