\documentclass{ximera}

\input{../../preamble.tex}

%\outcome{Describe the goals of optimization problems generally.}
%\outcome{Find all local maximums and minimums using the First and Second Derivative tests.}
%\outcome{Identify when we can find an absolute maximum or minimum on an open interval.}
%\outcome{Contrast optimization on open and closed intervals.}
%\outcome{Describe the objective function and constraints in a given optimization problem.}
%\outcome{Solve optimization problems by finding the appropriate extreme values.}

\author{Nela Lakos \and Kyle Parsons}

\begin{document}
\begin{exercise}

Consider the functions $f_A$, $f_B$, $f_C$, and $f_D$, each with domain $[1,5]$, graphed below.

\resizebox{0.45\textwidth}{!}{
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-0.3,xmax=5.3,ymin=-0.3,ymax=4.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-21,-20,...,21},
        xtick={-21,-20,...,21},
        xlabel=$x$, ylabel=$y$,
        y tick label style={anchor=west},
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick,penColor,domain=1:3] plot{sqrt(6-2*x)};
      \addplot[very thick,penColor,domain=3:5] plot{x-3};
      
	  \node at (axis cs:1,2) [penColor,yshift=-0.5pt] {$\bullet$};
	  \node at (axis cs:5,2) [penColor,yshift=-0.5pt] {$\bullet$};
       
      \node at (axis cs:1,2.5) {$y=f_A(x)$};
      \end{axis}`
  \end{tikzpicture}}
\hfill
\resizebox{0.45\textwidth}{!}{
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-0.3,xmax=5.3,ymin=-0.3,ymax=4.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-21,-20,...,21},
        xtick={-21,-20,...,21},
        xlabel=$x$, ylabel=$y$,
        y tick label style={anchor=west},
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick,penColor,domain=1:5] plot{5-x};
 
	  \addplot[only marks,mark=*,penColor] coordinates{(1,4) (5,0)};
 
      \node at (axis cs:1,2.5) {$y=f_B(x)$};
      \end{axis}`
  \end{tikzpicture}}
 
\resizebox{0.45\textwidth}{!}{
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-0.3,xmax=5.3,ymin=-0.3,ymax=4.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-21,-20,...,21},
        xtick={-21,-20,...,21},
        xlabel=$x$, ylabel=$y$,
        y tick label style={anchor=west},
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[penColor,very thick,domain=1:5,samples=50] plot{sqrt(x-1)};
      
      \addplot[only marks,mark=*,penColor] coordinates{(1,3) (5,2)};
      \addplot[only marks,mark=*,penColor,fill=white] coordinates{(1,0)};
       
      \node at (axis cs:1,2.5) {$y=f_C(x)$};
      \end{axis}`
  \end{tikzpicture}}
\hfill
\resizebox{0.45\textwidth}{!}{
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-0.3,xmax=5.3,ymin=-0.3,ymax=4.3,
        clip=true,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-21,-20,...,21},
        xtick={-21,-20,...,21},
        xlabel=$x$, ylabel=$y$,
        y tick label style={anchor=west},
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[thick,penColor,domain=1:5] plot{-(x-1)*(x-5)};
      
      \addplot[only marks,mark=*,penColor] coordinates{(1,2) (5,2)};
      \addplot[only marks,mark=*,penColor,fill=white] coordinates{(1,0) (5,0)};
             
      \node at (axis cs:1,2.5) {$y=f_D(x)$};
      \end{axis}`
  \end{tikzpicture}}

Select all functions that satisfy the conditions of the Mean Value Theorem on $[1,5]$.
\begin{selectAll}
\choice{$f_A(x)$}
\choice[correct]{$f_B(x)$}
\choice{$f_C(x)$}
\choice{$f_D(x)$}
\end{selectAll}

Select all functions $f$ for which there exists a point $c$ in $(1,5)$ such that
\[
f'(c) = \frac{f(5)-f(1)}{5-1}.
\]
\begin{selectAll}
\choice{$f_A(x)$}
\choice[correct]{$f_B(x)$}
\choice{$f_C(x)$}
\choice[correct]{$f_D(x)$}
\end{selectAll}


\end{exercise}
\end{document}