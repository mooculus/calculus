\documentclass{ximera}

\input{../../preamble}

\author{Nela Lakos \and Kyle Parsons}

\outcome{Know the graphs and properties of ``famous'' functions}

\begin{document}
\begin{exercise}

For the given famous function find their domain and range and match them with their graph.

\begin{itemize}
\item $y=\sin\left(x\right)$ Domain: $\left(\answer{-\infty},\answer{\infty}\right)$ Range: $\left[\answer{-1},\answer{1}\right]$ Graph: $\answer{A}$
\item $y=\cos\left(x\right)$ Domain: $\left(\answer{-\infty},\answer{\infty}\right)$ Range: $\left[\answer{-1},\answer{1}\right]$ Graph: $\answer{E}$
\item $y=e^x$ Domain: $\left(\answer{-\infty},\answer{\infty}\right)$ Range: $\left(\answer{0},\answer{\infty}\right)$ Graph: $\answer{D}$
\item $y=\ln\left(x\right)$ Domain: $\left(\answer{0},\answer{\infty}\right)$ Range: $\left(\answer{-\infty},\answer{\infty}\right)$ Graph: $\answer{C}$
\item $y=x$ Domain: $\left(\answer{-\infty},\answer{\infty}\right)$ Range: $\left(\answer{-\infty},\answer{\infty}\right)$ Graph: $\answer{F}$
\item $y=x^2$ Domain: $\left(\answer{-\infty},\answer{\infty}\right)$ Range: $\left[\answer{0},\answer{\infty}\right)$ Graph: $\answer{H}$
\item $y=\sqrt{x}$ Domain: $\left[\answer{0},\answer{\infty}\right)$ Range: $\left[\answer{0},\answer{\infty}\right)$ Graph: $\answer{B}$
\item $y=\left|x\right|$ Domain: $\left(\answer{-\infty},\answer{\infty}\right)$ Range: $\left[\answer{0},\answer{\infty}\right)$ Graph: $\answer{G}$
\end{itemize}

\begin{image}
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-4.3,xmax=4.3,ymin=-4.3,ymax=4.3,
        clip=false,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-4,-3,...,4},
    xtick={-4,-3,...,4},
        xlabel=$x$, ylabel=$y$,
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick, penColor, domain=-4.3:4.3,samples=100] {sin(180*x/pi)};

      \node[penColor] at (axis cs:-1.5, -2.5) [penColor] {$\Huge{A}$};
      \end{axis}`
  \end{tikzpicture}
\end{image}

\begin{image}
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-4.3,xmax=4.3,ymin=-4.3,ymax=4.3,
        clip=false,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-4,-3,...,4},
    xtick={-4,-3,...,4},
        xlabel=$x$, ylabel=$y$,
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick, penColor, domain=0:4.3,samples=100] {sqrt(x)};

      \node[penColor] at (axis cs:-1.5, -2.5) [penColor] {$\Huge{B}$};
      \end{axis}`
  \end{tikzpicture}
\end{image}

\begin{image}
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-4.3,xmax=4.3,ymin=-4.3,ymax=4.3,
        clip=false,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-4,-3,...,4},
    xtick={-4,-3,...,4},
        xlabel=$x$, ylabel=$y$,
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick, penColor, domain=e^(-4.3):4.3,samples=100] {ln(x)};

      \node[penColor] at (axis cs:-1.5, -2.5) [penColor] {$\Huge{C}$};
      \end{axis}`
  \end{tikzpicture}
\end{image}

\begin{image}
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-4.3,xmax=4.3,ymin=-4.3,ymax=4.3,
        clip=false,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-4,-3,...,4},
    xtick={-4,-3,...,4},
        xlabel=$x$, ylabel=$y$,
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick, penColor, domain=-4.3:ln(4.3),samples=100] {e^x};

      \node[penColor] at (axis cs:-1.5, -2.5) [penColor] {$\Huge{D}$};
      \end{axis}`
  \end{tikzpicture}
\end{image}

\begin{image}
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-4.3,xmax=4.3,ymin=-4.3,ymax=4.3,
        clip=false,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-4,-3,...,4},
    xtick={-4,-3,...,4},
        xlabel=$x$, ylabel=$y$,
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick, penColor, domain=-4.3:4.3,samples=50] {cos(180*x/pi)};

      \node[penColor] at (axis cs:-1.5, -2.5) [penColor] {$\Huge{E}$};
      \end{axis}`
  \end{tikzpicture}
\end{image}

\begin{image}
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-4.3,xmax=4.3,ymin=-4.3,ymax=4.3,
        clip=false,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-4,-3,...,4},
    xtick={-4,-3,...,4},
        xlabel=$x$, ylabel=$y$,
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick, penColor, domain=-4.3:4.3] {x};

      \node[penColor] at (axis cs:-1.5, -2.5) [penColor] {$\Huge{F}$};
      \end{axis}`
  \end{tikzpicture}
\end{image}

\begin{image}
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-4.3,xmax=4.3,ymin=-4.3,ymax=4.3,
        clip=false,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-4,-3,...,4},
    xtick={-4,-3,...,4},
        xlabel=$x$, ylabel=$y$,
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick, penColor, domain=0:4.3] {x};
      \addplot[very thick, penColor, domain=-4.3:0] {-x};

      \node[penColor] at (axis cs:-1.5, -2.5) [penColor] {$\Huge{G}$};
      \end{axis}`
  \end{tikzpicture}
\end{image}

\begin{image}
  \begin{tikzpicture}
    \begin{axis}[
        xmin=-4.3,xmax=4.3,ymin=-4.3,ymax=4.3,
        clip=false,
        unit vector ratio*=1 1 1,
        axis lines=center,
        grid = major,
        ytick={-4,-3,...,4},
    xtick={-4,-3,...,4},
        xlabel=$x$, ylabel=$y$,
        every axis y label/.style={at=(current axis.above origin),anchor=south},
        every axis x label/.style={at=(current axis.right of origin),anchor=west},
      ]
      \addplot[very thick, penColor, domain=-sqrt(4.3):sqrt(4.3),samples=50] {x^2};

      \node[penColor] at (axis cs:-1.5, -2.5) [penColor] {$\Huge{H}$};
      \end{axis}`
  \end{tikzpicture}
\end{image}


\end{exercise}
\end{document}