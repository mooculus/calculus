\documentclass{ximera}

\input{../../../preamble.tex}


\begin{document}

\textbf{For problems 11--14,} let $T$ be the solid tetrahedron with vertices
$$
(0,0,0), \hspace{1em}
(1,0,0), \hspace{1em}
(0,1,0), \hspace{1em}
(0,0,1).
$$

% outcome: set up a triple integral as an iterated integral
\begin{problem}
  Write down the \textbf{bounds for integrating} $F$ over $T$ in the order $\d z \d y \d x$.
  \vspace*{.4cm}
  \[
  \iiint_T F  \d V = \int_{\answer{0}}^{\answer{1}} \int_{\answer{0}}^{\answer{1-x}} \int_{\answer{0}}^{\answer{1-x-y}} F(x,y,z) \d z \d y \d x
  \]
  \vspace{.3cm}
\end{problem}

% evaluate a triple integral
\begin{problem}
  As a gesture of friendship, we tell you that $\iiint_T x \d V = 1/24$.
  \textbf{Evaluate:} \(\iiint_T (x+y+z) \d V \)
  \begin{prompt}
    \[
    \iiint_T (x+y+z) \d V = \answer{1/8}
    \]
  \end{prompt}

  \vfill
\end{problem}

% outcome: use symmetry to show that an integral vanishes
\begin{problem}
  The quantity \(\iiint_T (x - y) \, dV\) is (circle one):
  \begin{multipleChoice}
    \pdfOnly{\begin{multicols}{3}}
    \choice{positive}
    \choice{negative}
    \choice[correct]{zero}
    \pdfOnly{\end{multicols}}
  \end{multipleChoice}
\end{problem}

% outcome: compare integral by comparing integrand
\begin{problem}
  Circle the larger quantity:
  \begin{multipleChoice}
      \pdfOnly{\begin{multicols}{3}}
      \choice[correct]{$\iiint_T x^{2} \d V$}
      \choice{$\iiint_T x^{4} \d V$}
      \choice{Neither, they are equal.}
      \pdfOnly{\end{multicols}}
    \end{multipleChoice}
\end{problem}

\hrule
% outcome: surface area
\begin{problem}
  Let $R = \{ (x,y) : 0 \leq x \leq 1 \text{ and } 0 \leq y \leq 2
  \}$.  Suppose also that, whenever $(x,y) \in R$, then $\grad F(x,y)
  \approx \vector{4,8}$.  What is your best guess for the \textbf{surface area}
  of the graph of $F$ over the region $R$? 
  \begin{prompt}
    \[
    \text{surface area} =\answer{2\sqrt{81}}
    \]
  \end{prompt}
\end{problem}

\vfill

\hrule

\begin{problem}
  Consider the triangle with verticies at $(0,0)$, $(2,3)$, and
  $(3,1)$. \textbf{Set-up} sums of iterated integrals that will
  comupute the area of the triangle. Do this for integrating with respect to $x$ first and for integrating with respect to $y$ first. 
  \begin{prompt}
    \begin{align*}
      \text{area} &=\int_0^{\answer{1}} \int_{\answer{2y/3}}^{\answer{3y}} \d x \d y + \int_1^{\answer{3}} \int_{\answer{2y/3}}^{\answer{(7-y)/2}} \d x \d y\\
     &=\int_0^{\answer{2}} \int_{\answer{x/3}}^{\answer{3x/2}} \d y \d x +\int_2^{\answer{3}} \int_{\answer{x/3}}^{\answer{7-2x}} \d y \d x 
    \end{align*}
  \end{prompt}
  \vfill
\end{problem}

\end{document}
