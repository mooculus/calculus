\documentclass{ximera}
\input{../../preamble.tex}
\author{Steven Gubkin}
\license{Creative Commons 3.0 By-NC}

\outcome{Understand the definition of the derivative at a point.}
\outcome{Estimate the slope of the tangent line graphically.}
\outcome{Write the equation of the tangent line to a graph at a given point.}

\begin{document}
\begin{exercise}
Using the graph below, estimate   $ f'(2)$.

\begin{prompt}
$$\eval{\ddx f(x)}_{x =2} \approx \answer{0}$$
\end{prompt}

\begin{image}
          
\begin{tikzpicture}
	\begin{axis}[
            xmin=-4, xmax=4, ymin=-3,ymax=3,    
            unit vector ratio*=1 1 1,
            axis lines =middle, xlabel=$x$, ylabel=$y$,
            every axis y label/.style={at=(current axis.above origin),anchor=south},
            every axis x label/.style={at=(current axis.right of origin),anchor=west},
            xtick={-4,...,4}, ytick={-3,...,3},
            %xticklabels={-4,,-2,,0,,2,,4,,6}, yticklabels={,-2,,0,,2,,4,,6,,8,,10},
            grid=major,width=4in,
            grid style={dashed, gridColor},
          ]

          \addplot [very thick, penColor3!20!penColor2, samples=1000,smooth, domain=(-5:6)] {x*sin(deg(x))-2*sin(deg(2))+1};
          \addplot[color=penColor,fill=penColor,only marks,mark=*] coordinates{(2,1)};  %% closed hole


        \end{axis}
\end{tikzpicture}
\end{image}

Using your estimate above, what is the equation of the tangent line to $f$ at $x=2$?

\begin{prompt} 
The equation of this tangent line is
$$ y = \answer{1}$$ 
\end{prompt}

\end{exercise}
\end{document}
