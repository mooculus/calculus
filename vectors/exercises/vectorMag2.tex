\documentclass{ximera}

\input{../../preamble.tex}

\author{Bart Snapp}

\outcome{Calculate the magnitude of a vector.}

\begin{document}
\begin{exercise}
  Consider the vector $\vec{v}$ below:
  \begin{image}
    \begin{tikzpicture}
      \begin{axis}[
          xmin=-4,xmax=4,ymin=-4,ymax=4,
          clip=false,
          axis lines=center,
          unit vector ratio*=1 1 1,
          xlabel=$x$, ylabel=$y$,
          ytick={-4,-3,...,4},
	  xtick={-4,-3,...,4},
          grid = major,
          every axis y label/.style={at=(current axis.above origin),anchor=south},
          every axis x label/.style={at=(current axis.right of origin),anchor=west},
        ]
          \addplot[very thick,penColor,->] plot coordinates {(3,3) (-3,0)};
      \end{axis}
    \end{tikzpicture}
  \end{image}
  Compute $|\vec{v}|$.
  \begin{prompt}
    \[
    |\vec{v}|= \answer{3\sqrt{5}}
    \]
  \end{prompt}
\end{exercise}
\end{document}
